list1 = ["This", "is", "a"]
list2 = ["list"]
print(list1 + list2)

# You can concat 2 lists strings or tuples using the `+` operator
# you cannot concat 2 different sequence types and this causes a type error
